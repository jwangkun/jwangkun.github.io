<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://jwangkun.github.io</id>
    <title>John Wong&apos;s Blog</title>
    <updated>2020-10-15T03:16:59.291Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://jwangkun.github.io"/>
    <link rel="self" href="https://jwangkun.github.io/atom.xml"/>
    <subtitle>不忘初心</subtitle>
    <logo>https://jwangkun.github.io/images/avatar.png</logo>
    <icon>https://jwangkun.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, John Wong&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[k8s学习资料]]></title>
        <id>https://jwangkun.github.io/post/y5GbzS3T8/</id>
        <link href="https://jwangkun.github.io/post/y5GbzS3T8/">
        </link>
        <updated>2020-10-15T03:08:33.000Z</updated>
        <content type="html"><![CDATA[<ol>
<li>Kubernetes指南:https://kubernetes.feisky.xyz/<br>
2.Kubernetes官网:https://kubernetes.io/<br>
3.Kubernetes中文社区:https://www.kubernetes.org.cn/<br>
4.Kubernetes中文文档:https://kubernetes.io/zh/docs/home/<br>
5.Kubernetes官方文档:https://kubernetes.io/docs/home/<br>
6.书籍：《Kubernetes权威 指南》</li>
</ol>
<!-- more -->]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ K8s 资源全汇总 从零入门 K8s]]></title>
        <id>https://jwangkun.github.io/post/Z-wMIR6wz/</id>
        <link href="https://jwangkun.github.io/post/Z-wMIR6wz/">
        </link>
        <updated>2020-10-15T03:04:12.000Z</updated>
        <summary type="html"><![CDATA[<p>从 2019 年，云原生技术开始大规模普及，以“云”为核心的软件研发思想，正逐步成为所有开发者的默认选项，像 Kubernetes 等云原生技术正在成为技术人员的必修课。这种背景下，“会 Kubernetes”已经远远不够了，“懂 Kubernetes”、“会云原生架构”的重要性正日益凸显出来，这也是为什么大家都要学习和投资云原生技术的重要原因。</p>
]]></summary>
        <content type="html"><![CDATA[<p>从 2019 年，云原生技术开始大规模普及，以“云”为核心的软件研发思想，正逐步成为所有开发者的默认选项，像 Kubernetes 等云原生技术正在成为技术人员的必修课。这种背景下，“会 Kubernetes”已经远远不够了，“懂 Kubernetes”、“会云原生架构”的重要性正日益凸显出来，这也是为什么大家都要学习和投资云原生技术的重要原因。</p>
<!-- more -->
<h3 id="课程介绍">【课程介绍】</h3>
<p>本课程是由 CNCF 官方与阿里巴巴强强联合，共同推出的以“云原生技术体系”为核心、以“技术解读”和“实践落地”并重的系列技术公开课。课程全程免费且无需注册，为您带来：</p>
<ul>
<li>完善的知识体系，打造属于自己的云原生技能树</li>
<li>理解云原生技术背后的思想与本质</li>
<li>与知识体系相辅相成的动手实践</li>
<li>一线技术团队云原生技术最佳实践</li>
</ul>
<h3 id="讲师阵容">【讲师阵容】</h3>
<p>来自全球“云原生”技术社区的亲历者和领军人物，如作为 <strong>CNCF 全球 TOC 之一的李响及 CNCF 官方大使张磊</strong>，齐聚“课堂”为每一位中国开发者讲解和剖析关于“云原生”的方方面面，旨在让广大中国开发者可以近距离聆听世界级技术专家解析云原生技术，让“云原生”技术真正触手可及。</p>
<h3 id="资源汇总">【资源汇总】</h3>
<ul>
<li><strong>课时1</strong> 《Kubernetes 入门必备云原生发展简史》</li>
</ul>
<p>图文版课程链接：<a href="https://developer.aliyun.com/article/717581">developer.aliyun.com/article/717…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13076">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时2</strong> 《从零开始入门 K8s | 详解 K8s 容器基本概念》</li>
</ul>
<p>图文版课程链接：<a href="https://developer.aliyun.com/article/718433">developer.aliyun.com/article/718…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13082">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时3 **《从零开始入门 K8s | 阿里技术专家详解 K8s 核心概念》</li>
</ul>
<p>图文版课程链接：<a href="https://developer.aliyun.com/article/718798">developer.aliyun.com/article/718…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13078">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时4</strong> 《从零入门 K8s | 人人都能看懂 Pod 与容器设计模式》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/OW7zvGhPgGAnBuo4A_SXFw">mp.weixin.qq.com/s/OW7zvGhPg…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13079">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时5</strong> 《从零开始入门 K8s | K8s 的应用编排与管理》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/T1l5ebHqo0GUFfbfBnd-tQ">mp.weixin.qq.com/s/T1l5ebHqo…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13080">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时6</strong> 《从零开始入门 K8s | 应用编排与管理》</li>
</ul>
<p>图文版课程链接：<a href="https://developer.aliyun.com/article/719455">developer.aliyun.com/article/719…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13081">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时7 **《从零开始入门 K8s | 应用编排与管理：Job &amp; DaemonSet》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/XdhdAxaLPg7ZUS5WonXhXw">mp.weixin.qq.com/s/XdhdAxaLP…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13083">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时8</strong> 《从零开始入门 K8s | 如何实现应用配置管理？》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/8r-_Ekje__GVHsKLfJ-66A">mp.weixin.qq.com/s/8r-<em>Ekje</em>…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13084">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时9 **《从零开始入门 K8s | 应用存储和持久化数据卷：核心知识》</li>
</ul>
<p>图文版课程链接：<a href="https://developer.aliyun.com/article/720251">developer.aliyun.com/article/720…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13085">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时10</strong> 《从零开始入门 K8s | 应用存储和持久化数据卷：存储快照与拓扑调度》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/enoy5KRd2sxpZFZ6f06ZiQ">mp.weixin.qq.com/s/enoy5KRd2…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_15457">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时11</strong> 《从零开始入门 K8s | 可观测性：你的应用健康吗？》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/L1KT0VISxlGYZXGtLzSwBA">mp.weixin.qq.com/s/L1KT0VISx…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13086">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时12 **《从零开始入门 K8s | 可观测性：监控与日志》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/5z4IhCJhCfwbSZne93VEsQ">mp.weixin.qq.com/s/5z4IhCJhC…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_15541">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时13</strong> 《从零开始入门 K8s | Kubernetes 网络概念及策略控制》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/kjOAlKTwaMZzVOiSuJE6fQ">mp.weixin.qq.com/s/kjOAlKTwa…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13087">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时14</strong> 《从零开始入门 | Kubernetes 中的服务发现与负载均衡》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/klc0GYAcTthPdUaF-O7izQ">mp.weixin.qq.com/s/klc0GYAcT…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_15658">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时15</strong> 《从零开始入门 K8s | 深入剖析 Linux 容器》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/r3BCE46iAMn4gqbY3lS6TA">mp.weixin.qq.com/s/r3BCE46iA…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13088">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>特别篇</strong> 《Helm 从入门到实践 | 从 0 开始制作一个 Helm Charts》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/uWdPB4LvmMmvUX2ioKwbww">mp.weixin.qq.com/s/uWdPB4Lvm…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_16513">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时16</strong> 《从零开始入门 K8s | 手把手带你理解 etcd》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/qjLevhAn43d6jWQR_WTMJw">mp.weixin.qq.com/s/qjLevhAn4…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13089">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时17 **《从零开始入门 K8s | etcd 性能优化实践》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/lD2b-DZyvRJ3qWqmlvHpxg">mp.weixin.qq.com/s/lD2b-DZyv…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_16861">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时18 **《从零开始入门 K8s | Kubernetes 调度和资源管理》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/yD7vmIq9iCGrwAUKHs9WqA">mp.weixin.qq.com/s/yD7vmIq9i…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13090">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时19</strong> 《从零开始入门 K8s | 调度器的调度流程和算法介绍》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/1ntzs0__3FnLJRE9MDDCnQ">mp.weixin.qq.com/s/1ntzs0__3…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_17018">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时20</strong> 《从零开始入门 K8s | GPU 管理和 Device Plugin 工作机制》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/-rUaFgTbI-GI-LmJTkferQ">mp.weixin.qq.com/s/-rUaFgTbI…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13091">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时21</strong> 《从零开始入门 K8s | Kubernetes 存储架构及插件使用》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/QWLGkpqpMdsY1w6npZj-yQ">mp.weixin.qq.com/s/QWLGkpqpM…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13092">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时22 **《从零开始入门 K8s | 有状态应用编排 - StatefulSet》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/oTNv8gop93ljEUGhfKzNrw">mp.weixin.qq.com/s/oTNv8gop9…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13093">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时23</strong> 《从零开始入门 K8s | Kubernetes API 编程范式》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/uBCNTLCllE0tl70oLhLufg">mp.weixin.qq.com/s/uBCNTLCll…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13094">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时24 **《从零开始入门 K8s | Kubernetes API 编程利器：Operator 和 Operator Framework》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/SdWp1A6zDp7inLzG5VSE4Q">mp.weixin.qq.com/s/SdWp1A6zD…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13095">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时25</strong> 《从零开始入门 K8s | Kubernetes 网络模型进阶》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/Jm8VynGd506wN5-yiLHzdg">mp.weixin.qq.com/s/Jm8VynGd5…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13096">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时26 **《从零开始入门 K8s | 理解 CNI 和 CNI 插件》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/sGTEp9m8PC2zhlEgcnqtZA">mp.weixin.qq.com/s/sGTEp9m8P…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13097">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时27</strong> 《从零开始入门 K8s | K8s 安全之访问控制》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/nPErpcghHih5-dGPQkStJA">mp.weixin.qq.com/s/nPErpcghH…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13099">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li>**课时28 **《从零开始入门 K8s | 理解容器运行时接口 CRI》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/aexS9HmlxCpmY7gn3MIFsA">mp.weixin.qq.com/s/aexS9Hmlx…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_17238">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时29</strong> 《从零开始入门 K8s | Kata Containers 创始人带你入门安全容器技术》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/w2SkC6TuSBqurvAae0RAUA">mp.weixin.qq.com/s/w2SkC6TuS…</a><br>
视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13100">edu.aliyun.com/lesson_1651…</a></p>
<ul>
<li><strong>课时30</strong> 《从零开始入门 K8s | 理解 RuntimeClass 与使用多容器运行时》</li>
</ul>
<p>图文版课程链接：<a href="https://mp.weixin.qq.com/s/JQ2NNHmpUrVUqX6BIyqUmQ">mp.weixin.qq.com/s/JQ2NNHmpU…</a> 视频版课程链接：<a href="https://edu.aliyun.com/lesson_1651_13101">edu.aliyun.com/lesson_1651…</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[kubectl基本命令]]></title>
        <id>https://jwangkun.github.io/post/HwKdS3aOl/</id>
        <link href="https://jwangkun.github.io/post/HwKdS3aOl/">
        </link>
        <updated>2020-10-15T02:54:21.000Z</updated>
        <summary type="html"><![CDATA[<h3 id="设置命令自动补全">设置命令自动补全</h3>
<pre><code class="language-bash"># 首先下载这个命令，补全命令才能生效
yum install -y bash-completion
# 1、当前shell生效
yum install -y bash-completion
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<h3 id="设置命令自动补全">设置命令自动补全</h3>
<pre><code class="language-bash"># 首先下载这个命令，补全命令才能生效
yum install -y bash-completion
# 1、当前shell生效
yum install -y bash-completion

&lt;!-- more --&gt;

#2、总是会生效
# vi  .bashrc    (/etc/profile)
source &lt;(kubectl completion bash)

source ~/.bashrc
</code></pre>
<h3 id="对节点的操作">对节点的操作</h3>
<p>获取集群节点信息</p>
<pre><code class="language-bash">kubectl get nodes  # 获取集群节点信息

# 输出不同的格式
kubectl get nodes -o wide
kubectl get nodes -o json
kubectl get nodes -o yaml

kubectl get nodes node2 -o wide # 查看某个节点的信息
</code></pre>
<p>增加节点</p>
<pre><code class="language-bash"># 在主节点上master执行命令
# kubeadm token create --print-join-command
# node节点上执行上述命令输出的内容
kubeadm join apiserver.demo:6443 --token l9y210.bzx98ovq27i7an20     --discovery-token-ca-cert-hash sha256:274bcf2916f6c94b594098043aa697aef5137941b4e8f972f9183547d88323b3
</code></pre>
<p>移除节点</p>
<pre><code class="language-bash"># 只在 worker 节点执行
kubeadm reset
# 只在 master 节点执行
kubectl delete node demo-worker-x-x
</code></pre>
<h3 id="对节点标签进行操作">对节点标签进行操作</h3>
<p>获取标签信息</p>
<pre><code class="language-bash">kubectl get nodes --show-labels
kubectl get nodes node2 --show-labels # 查看某个节点的标签信息
</code></pre>
<p>添加标签</p>
<pre><code class="language-bash"># 标签以key=value的形式存在

# 设置node2节点的label是  disktype: node2
kubectl label  nodes  node2 disktype:node2
#会输出 node2 labeled

kubectl get nodes node2 --show-labels
# 输出节点对应的内容  查看输出label内容   
</code></pre>
<p>删除标签</p>
<pre><code class="language-bash">kubectk label nodes node2 disktype-
# 删除标签，只需要对应标签的key加减号(-)
</code></pre>
<h3 id="设置角色">设置角色</h3>
<pre><code class="language-bash"># 刚加入集群  node的角色定义是none，我们可以查看master的标签，查看角色对应的label
kubectl get nodes master --show-labels
#会看到 node-role.kubernetes.io/master= 这样的一个标签
# 修改对应的角色即可

#将node1，node2的角色定义为worker
kubectl label nodes node1 node-role.kubernetes.io/worker=
kubectl label nodes node2 node-role.kubernetes.io/worker=
</code></pre>
<h3 id="对pod的操作">对pod的操作</h3>
<p>创建pod</p>
<pre><code class="language-bash">kubectl  create -f xxx.yml
</code></pre>
<p>删除pod</p>
<pre><code class="language-bash">kubectl delete -f xxx.yml
</code></pre>
<p>获取pod信息</p>
<pre><code class="language-bash">kubectl get pods  # 获取当前名称空间的pod
kubectl get pods --namespace demo  # 获取指定名称空间的pod
</code></pre>
<p>进入指定pod的指定的容器内</p>
<pre><code class="language-bash">#kubectl exec -it pod_name -c container_name sh
kubectl exec -it nginx -c nginx sh
</code></pre>
<h3 id="namespace">namespace</h3>
<p>获取命名空间</p>
<pre><code class="language-bash">kubectl get namespace
</code></pre>
<p>创建命名空间</p>
<pre><code class="language-bash"># kubectl create namespace namespace_name
kubectl create namespace demo
</code></pre>
<p>删除命名空间</p>
<pre><code class="language-bash"># kubectl delete namespace namespace_name
kubectl delete namespace demo
</code></pre>
<p>创建一个指定命令空间的pod的yaml文件</p>
<pre><code class="language-bash">apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: demo  # 指定名称空间
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
</code></pre>
<pre><code class="language-bash">kubectl create namespace demo # 创建对应的名称空间
kubectl create -f nginx_namespace.yml
kubectl get pods # 在当前名称空间查看pod，查找不到刚才创建的pod
kubectl get pods --namespace demo # 查看指定名称空间的pod
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Docker 常用命令汇总]]></title>
        <id>https://jwangkun.github.io/post/Ij5JN2yIV/</id>
        <link href="https://jwangkun.github.io/post/Ij5JN2yIV/">
        </link>
        <updated>2020-10-15T02:38:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="docker-镜像命令">Docker 镜像命令</h3>
<p>列出镜像列表</p>
<pre><code class="language-sh">$ docker images
$ docker image ls -a
</code></pre>
<p>运行 Docker 镜像（守护态方式）</p>
<pre><code class="language-sh">$ docker run -d {镜像名}
</code></pre>
<p>删除指定 Docker 镜像</p>
<pre><code class="language-sh">$ docker  image rm {镜像名}
</code></pre>
<p>删除 Docker 虚悬镜像</p>
<pre><code class="language-sh">$ docker image prune
</code></pre>
<h3 id="docker-容器命令">Docker 容器命令</h3>
<p>列出正在运行的容器</p>
<pre><code class="language-bash">$ docker ps -a
</code></pre>
<p>列出所有容器（包括已停止容器）</p>
<pre><code class="language-bash">$ docker ps -l
</code></pre>
<p>进入运行中的 Docker 容器</p>
<pre><code class="language-bash">$ docker exec -it {容器ID} /bin/bash
</code></pre>
<p>停止 Docker 容器</p>
<pre><code class="language-bash">$ docker stop {容器ID}
</code></pre>
<p>删除指定 Docker 容器</p>
<pre><code class="language-bash">$ docker rm -f {容器ID}
</code></pre>
<p>删除停止的 Docker 容器</p>
<pre><code class="language-bash">$ docker container prune
</code></pre>
<p>查看 Docker 容器历史运行日志</p>
<pre><code class="language-bash">$ docker logs {容器名}
</code></pre>
<p>实时监听 Docker 容器运行日志</p>
<pre><code class="language-bash">$ docker logs -f {容器名}
</code></pre>
<h3 id="docker-数据卷命令">Docker 数据卷命令</h3>
<p>创建 Docker 数据卷</p>
<pre><code class="language-bash">$ docker volume create {数据卷名}
</code></pre>
<p>列出所有 Docker 数据卷</p>
<pre><code class="language-bash">$ docker volume ls
</code></pre>
<p>删除指定 Docker 数据卷</p>
<pre><code class="language-bash">$ docker volume rm {数据卷名}
</code></pre>
<p>删除未关联（失效） Docker 数据卷</p>
<pre><code class="language-bash">$ docker volume prune
$ docker volume rm $(docker volume ls -qf dangling=true)
</code></pre>
<h3 id="docker-文件操作命令">Docker 文件操作命令</h3>
<p>从主机复制文件到 Docker 容器中</p>
<pre><code class="language-bash">$ sudo docker cp {主机内文件路径} {容器ID}:{容器内文件存储路径}
</code></pre>
<p>从 Docker 容器中复制文件到主机中</p>
<pre><code class="language-bash">$ sudo docker cp {容器ID}:{容器内文件路径} {主机内文件存储路径}
</code></pre>
<h2 id="从image启动一个container">从image启动一个container</h2>
<p>docker run命令首先会从特定的image创之上create一层可写的container，然后通过start命令来启动它。停止的container可以重新启动并保留原来的修改。run命令启动参数有很多，以下是一些常规使用说明</p>
<p>当利用 docker run 来创建容器时，Docker 在后台运行的标准操作包括：</p>
<ul>
<li>检查本地是否存在指定的镜像，不存在就从公有仓库下载</li>
<li>利用镜像创建并启动一个容器</li>
<li>分配一个文件系统，并在只读的镜像层外面挂载一层可读写层</li>
<li>从宿主主机配置的网桥接口中桥接一个虚拟接口到容器中去</li>
<li>从地址池配置一个 ip 地址给容器</li>
<li>执行用户指定的应用程序</li>
<li>执行完毕后容器被终止</li>
</ul>
<pre><code>Usage: docker run [OPTIONS] IMAGE [COMMAND] [ARG...]
</code></pre>
<p>下面是一个例子：</p>
<pre><code>$ sudo docker run --name nginx_test \
&gt; -v /tmp/docker:/usr/share/nginx/html:ro \
&gt; -p 80:80 -d \
&gt; nginx:1.7.6
</code></pre>
<h2 id="开启停止重启containerstartstoprestart">开启/停止/重启container（start/stop/restart</h2>
<p>容器可以通过run新建一个来运行，也可以重新start已经停止的container，但start不能够再指定容器启动时运行的指令，因为docker只能有一个前台进程。</p>
<p>容器stop（或Ctrl+D）时，会在保存当前容器的状态之后退出，下次start时保有上次关闭时更改。而且每次进入attach进去的界面是一样的，与第一次run启动或commit提交的时刻相同。</p>
<pre><code>CONTAINER_ID=$(docker start &lt;containner_id&gt;)
$ sudo docker stop $CONTAINER_ID
$ sudo docker restart $CONTAINER_ID
</code></pre>
<h2 id="连接到正在运行中的containerattach">连接到正在运行中的container（attach）</h2>
<p>要attach上去的容器必须正在运行，可以同时连接上同一个container来共享屏幕（与screen命令的attach类似）。</p>
<p>官方文档中说attach后可以通过CTRL-C来detach，但实际上经过我的测试，如果container当前在运行bash，CTRL-C自然是当前行的输入，没有退出；如果container当前正在前台运行进程，如输出nginx的access.log日志，CTRL-C不仅会导致退出容器，而且还stop了。这不是我们想要的，detach的意思按理应该是脱离容器终端，但容器依然运行。好在attach是可以带上--sig-proxy=false来确保CTRL-D或CTRL-C不会关闭容器。</p>
<pre><code>$ sudo docker attach --sig-proxy=false $CONTAINER_ID
</code></pre>
<h2 id="查看image或container的底层信息inspect">查看image或container的底层信息（inspect）</h2>
<p>inspect的对象可以是image、运行中的container和停止的container。</p>
<p>查看容器的内部IP</p>
<pre><code>$ sudo docker inspect --format='{{.NetworkSettings.IPAddress}}' $CONTAINER_ID
</code></pre>
<h2 id="查看容器中正在运行的进程top">查看容器中正在运行的进程（top）</h2>
<p>容器运行时不一定有/bin/bash终端来交互执行top命令，查看container中正在运行的进程，况且还不一定有top命令，这是docker top &lt;container_id/container_name&gt;就很有用了。实际上在host上使用ps -ef|grep docker也可以看到一组类似的进程信息，把container里的进程看成是host上启动docker的子进程就对了。</p>
<pre><code>$ sudo docker top &lt;container_id/container_name&gt;
</code></pre>
<h2 id=""></h2>
<h2 id="docker-build-命令">Docker build 命令</h2>
<p>build命令可以从Dockerfile和上下文来创建镜像：</p>
<pre><code>$ sudo docker build [OPTIONS] PATH | URL | -
</code></pre>
<p>上面的PATH或URL中的文件被称作上下文，build image的过程会先把这些文件传送到docker的服务端来进行的。</p>
<pre><code># cat Dockerfile 
 FROM seanlook/nginx:bash_vim
 EXPOSE 80
 ENTRYPOINT /usr/sbin/nginx -c /etc/nginx/nginx.conf &amp;&amp; /bin/bash

# docker build -t seanlook/nginx:bash_vim_Df .
</code></pre>
<p>给镜像打上标签（tag）</p>
<p>tag的作用主要有两点：一是为镜像起一个容易理解的名字，二是可以通过docker tag来重新指定镜像的仓库，这样在push时自动提交到仓库。</p>
<p>将同一IMAGE_ID的所有tag，合并为一个新的</p>
<pre><code>$ sudo docker tag 195eb90b5349 seanlook/ubuntu:rm_test
</code></pre>
<p>新建一个tag，保留旧的那条记录</p>
<pre><code>$ sudo docker tag Registry/Repos:Tag New_Registry/New_Repos:New_Tag
</code></pre>
<h2 id="将一个container固化为一个新的imagecommit">将一个container固化为一个新的image（commit）</h2>
<p>当我们在制作自己的镜像的时候，会在container中安装一些工具、修改配置，如果不做commit保存起来，那么container停止以后再启动，这些更改就消失了。</p>
<pre><code>$ sudo docker commit &lt;container&gt; [repo:tag]
</code></pre>
<p>后面的repo:tag可选</p>
<p>只能提交正在运行的container，即通过docker ps可以看见的容器</p>
<h2 id="推送一个image或repository到registrypush">推送一个image或repository到registry（push）</h2>
<p>与上面的pull对应，可以推送到Docker Hub的Public、Private以及私服，但不能推送到Top Level Repository。</p>
<pre><code>$ sudo docker push seanlook/mongo
$ sudo docker push registry.tp-link.net:5000/mongo:2014-10-27
</code></pre>
<p>registry.tp-link.net也可以写成IP，172.29.88.222。</p>
<p>在repository不存在的情况下，命令行下push上去的会为我们创建为私有库，然而通过浏览器创建的默认为公共库。</p>
<h2 id="其他命令"><strong>其他命令</strong></h2>
<p>docker还有一些如login、cp、logs、export、import、load、kill等不是很常用的命令，比较简单，请参考官网。</p>
<p>events、history和logs命令</p>
<p>这3个命令用于查看Docker的系统日志信息。events命令会打印出实时的系统事件；history命令会打印出指定镜像的历史版本信息，即构建该镜像的每一层镜像的命令记录；logs命令会打印出容器中进程的运行日志。</p>
<p>docker events [options] ：从服务器获取实时事件。</p>
<ul>
<li>
<pre><code>Options：

- **-f** **：**根据条件过滤事件；
- **--since** **：**从指定的时间戳后显示所有事件;
- **--until** **：**流水时间显示到指定的时间为止；
</code></pre>
</li>
</ul>
<p>docker history [options] image：查看指定镜像的创建历史。</p>
<ul>
<li>
<pre><code>Options：

- **-H :**以可读的格式打印镜像大小和日期，默认为true；
- **--no-trunc :**显示完整的提交记录；
- **-q :**仅列出提交记录ID。
</code></pre>
</li>
</ul>
<p>docker logs [options] container</p>
<pre><code>Options:

     --details    显示更多的信息
   -f, --follow     跟踪日志输出，最后一行为当前时间戳的日志
     --since string  显示自具体某个时间或时间段的日志
     --tail string  从日志末尾显示多少行日志， 默认是all
   -t, --timestamps   显示时间戳
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用kubeadm快速部署一个K8s集群]]></title>
        <id>https://jwangkun.github.io/post/fdMXY3LCV/</id>
        <link href="https://jwangkun.github.io/post/fdMXY3LCV/">
        </link>
        <updated>2020-10-15T00:39:13.000Z</updated>
        <summary type="html"><![CDATA[<p>kubeadm是官方社区推出的一个用于快速部署kubernetes集群的工具。</p>
<p>这个工具能通过两条指令完成一个kubernetes集群的部署：</p>
<pre><code># 创建一个 Master 节点
$ kubeadm init

# 将一个 Node 节点加入到当前集群中
$ kubeadm join &lt;Master节点的IP和端口 &gt;
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<p>kubeadm是官方社区推出的一个用于快速部署kubernetes集群的工具。</p>
<p>这个工具能通过两条指令完成一个kubernetes集群的部署：</p>
<pre><code># 创建一个 Master 节点
$ kubeadm init

# 将一个 Node 节点加入到当前集群中
$ kubeadm join &lt;Master节点的IP和端口 &gt;
</code></pre>
<!-- more -->
<h2 id="1-安装要求">1. 安装要求</h2>
<p>在开始之前，部署Kubernetes集群机器需要满足以下几个条件：</p>
<ul>
<li>一台或多台机器，操作系统 CentOS7.x-86_x64</li>
<li>硬件配置：2GB或更多RAM，2个CPU或更多CPU，硬盘30GB或更多</li>
<li>可以访问外网，需要拉取镜像，如果服务器不能上网，需要提前下载镜像并导入节点</li>
<li>禁止swap分区</li>
</ul>
<h2 id="2-准备环境">2. 准备环境</h2>
<table>
<thead>
<tr>
<th>角色</th>
<th>IP</th>
</tr>
</thead>
<tbody>
<tr>
<td>master</td>
<td>192.168.1.11</td>
</tr>
<tr>
<td>node1</td>
<td>192.168.1.12</td>
</tr>
<tr>
<td>node2</td>
<td>192.168.1.13</td>
</tr>
</tbody>
</table>
<pre><code># 关闭防火墙
systemctl stop firewalld
systemctl disable firewalld

# 关闭selinux
sed -i 's/enforcing/disabled/' /etc/selinux/config  # 永久
setenforce 0  # 临时

# 关闭swap
swapoff -a  # 临时
sed -ri 's/.*swap.*/#&amp;/' /etc/fstab    # 永久

# 根据规划设置主机名
hostnamectl set-hostname &lt;hostname&gt;

# 在master添加hosts
cat &gt;&gt; /etc/hosts &lt;&lt; EOF
192.168.44.146 k8smaster
192.168.44.145 k8snode1
192.168.44.144 k8snode2
EOF

# 将桥接的IPv4流量传递到iptables的链
cat &gt; /etc/sysctl.d/k8s.conf &lt;&lt; EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
EOF
sysctl --system  # 生效

# 时间同步
yum install ntpdate -y
ntpdate time.windows.com
</code></pre>
<h2 id="3-所有节点安装dockerkubeadmkubelet">3. 所有节点安装Docker/kubeadm/kubelet</h2>
<p>Kubernetes默认CRI（容器运行时）为Docker，因此先安装Docker。</p>
<h3 id="31-安装docker">3.1 安装Docker</h3>
<pre><code>$ wget https://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo -O /etc/yum.repos.d/docker-ce.repo
$ yum -y install docker-ce-18.06.1.ce-3.el7
$ systemctl enable docker &amp;&amp; systemctl start docker
$ docker --version
Docker version 18.06.1-ce, build e68fc7a
</code></pre>
<pre><code>$ cat &gt; /etc/docker/daemon.json &lt;&lt; EOF
{
  &quot;registry-mirrors&quot;: [&quot;https://b9pmyelo.mirror.aliyuncs.com&quot;]
}
EOF
</code></pre>
<h3 id="32-添加阿里云yum软件源">3.2 添加阿里云YUM软件源</h3>
<pre><code>$ cat &gt; /etc/yum.repos.d/kubernetes.repo &lt;&lt; EOF
[kubernetes]
name=Kubernetes
baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64
enabled=1
gpgcheck=0
repo_gpgcheck=0
gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg
EOF
</code></pre>
<h3 id="33-安装kubeadmkubelet和kubectl">3.3 安装kubeadm，kubelet和kubectl</h3>
<p>由于版本更新频繁，这里指定版本号部署：</p>
<pre><code>$ yum install -y kubelet-1.18.0 kubeadm-1.18.0 kubectl-1.18.0
$ systemctl enable kubelet
</code></pre>
<h2 id="4-部署kubernetes-master">4. 部署Kubernetes Master</h2>
<p>在192.168.31.61（Master）执行。</p>
<pre><code>$ kubeadm init \
  --apiserver-advertise-address=192.168.44.146 \
  --image-repository registry.aliyuncs.com/google_containers \
  --kubernetes-version v1.18.0 \
  --service-cidr=10.96.0.0/12 \
  --pod-network-cidr=10.244.0.0/16
</code></pre>
<p>由于默认拉取镜像地址k8s.gcr.io国内无法访问，这里指定阿里云镜像仓库地址。</p>
<p>使用kubectl工具：</p>
<pre><code class="language-bash">mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
$ kubectl get nodes
</code></pre>
<h2 id="5-加入kubernetes-node">5. 加入Kubernetes Node</h2>
<p>在192.168.1.12/13（Node）执行。</p>
<p>向集群添加新节点，执行在kubeadm init输出的kubeadm join命令：</p>
<pre><code>$ kubeadm join 192.168.1.11:6443 --token esce21.q6hetwm8si29qxwn \
    --discovery-token-ca-cert-hash sha256:00603a05805807501d7181c3d60b478788408cfe6cedefedb1f97569708be9c5
</code></pre>
<p>默认token有效期为24小时，当过期之后，该token就不可用了。这时就需要重新创建token，操作如下：</p>
<pre><code>kubeadm token create --print-join-command
</code></pre>
<h2 id="6-部署cni网络插件">6. 部署CNI网络插件</h2>
<pre><code>wget https://raw.githubusercontent.com/coreos/flannel/master/Documentation/kube-flannel.yml
</code></pre>
<p>默认镜像地址无法访问，sed命令修改为docker hub镜像仓库。</p>
<pre><code>kubectl apply -f https://raw.githubusercontent.com/coreos/flannel/master/Documentation/kube-flannel.yml

kubectl get pods -n kube-system
NAME                          READY   STATUS    RESTARTS   AGE
kube-flannel-ds-amd64-2pc95   1/1     Running   0          72s
</code></pre>
<h2 id="7-测试kubernetes集群">7. 测试kubernetes集群</h2>
<p>在Kubernetes集群中创建一个pod，验证是否正常运行：</p>
<pre><code>$ kubectl create deployment nginx --image=nginx
$ kubectl expose deployment nginx --port=80 --type=NodePort
$ kubectl get pod,svc
</code></pre>
<p>访问地址：http://NodeIP:Port</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用kubeadm搭建高可用的K8s集群]]></title>
        <id>https://jwangkun.github.io/post/qMQ3HyNA5/</id>
        <link href="https://jwangkun.github.io/post/qMQ3HyNA5/">
        </link>
        <updated>2020-10-15T00:38:13.000Z</updated>
        <summary type="html"><![CDATA[<p>kubeadm是官方社区推出的一个用于快速部署kubernetes集群的工具。</p>
<p>这个工具能通过两条指令完成一个kubernetes集群的部署：</p>
<pre><code># 创建一个 Master 节点
$ kubeadm init

# 将一个 Node 节点加入到当前集群中
$ kubeadm join &lt;Master节点的IP和端口 &gt;
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<p>kubeadm是官方社区推出的一个用于快速部署kubernetes集群的工具。</p>
<p>这个工具能通过两条指令完成一个kubernetes集群的部署：</p>
<pre><code># 创建一个 Master 节点
$ kubeadm init

# 将一个 Node 节点加入到当前集群中
$ kubeadm join &lt;Master节点的IP和端口 &gt;
</code></pre>
<!-- more -->
<h2 id="1-安装要求">1. 安装要求</h2>
<p>在开始之前，部署Kubernetes集群机器需要满足以下几个条件：</p>
<ul>
<li>一台或多台机器，操作系统 CentOS7.x-86_x64</li>
<li>硬件配置：2GB或更多RAM，2个CPU或更多CPU，硬盘30GB或更多</li>
<li>可以访问外网，需要拉取镜像，如果服务器不能上网，需要提前下载镜像并导入节点</li>
<li>禁止swap分区</li>
</ul>
<h2 id="2-准备环境">2. 准备环境</h2>
<table>
<thead>
<tr>
<th>角色</th>
<th>IP</th>
</tr>
</thead>
<tbody>
<tr>
<td>master1</td>
<td>192.168.44.155</td>
</tr>
<tr>
<td>master2</td>
<td>192.168.44.156</td>
</tr>
<tr>
<td>node1</td>
<td>192.168.44.157</td>
</tr>
<tr>
<td>VIP（虚拟ip）</td>
<td>192.168.44.158</td>
</tr>
</tbody>
</table>
<pre><code># 关闭防火墙
systemctl stop firewalld
systemctl disable firewalld

# 关闭selinux
sed -i 's/enforcing/disabled/' /etc/selinux/config  # 永久
setenforce 0  # 临时

# 关闭swap
swapoff -a  # 临时
sed -ri 's/.*swap.*/#&amp;/' /etc/fstab    # 永久

# 根据规划设置主机名
hostnamectl set-hostname &lt;hostname&gt;

# 在master添加hosts
cat &gt;&gt; /etc/hosts &lt;&lt; EOF
192.168.44.158    master.k8s.io   k8s-vip
192.168.44.155    master01.k8s.io master1
192.168.44.156    master02.k8s.io master2
192.168.44.157    node01.k8s.io   node1
EOF

# 将桥接的IPv4流量传递到iptables的链
cat &gt; /etc/sysctl.d/k8s.conf &lt;&lt; EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
EOF
sysctl --system  # 生效

# 时间同步
yum install ntpdate -y
ntpdate time.windows.com
</code></pre>
<h2 id="3-所有master节点部署keepalived">3. 所有master节点部署keepalived</h2>
<h3 id="31-安装相关包和keepalived">3.1 安装相关包和keepalived</h3>
<pre><code>yum install -y conntrack-tools libseccomp libtool-ltdl

yum install -y keepalived
</code></pre>
<h3 id="32配置master节点">3.2配置master节点</h3>
<p>master1节点配置</p>
<pre><code>cat &gt; /etc/keepalived/keepalived.conf &lt;&lt;EOF 
! Configuration File for keepalived

global_defs {
   router_id k8s
}

vrrp_script check_haproxy {
    script &quot;killall -0 haproxy&quot;
    interval 3
    weight -2
    fall 10
    rise 2
}

vrrp_instance VI_1 {
    state MASTER 
    interface ens33 
    virtual_router_id 51
    priority 250
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass ceb1b3ec013d66163d6ab
    }
    virtual_ipaddress {
        192.168.44.158
    }
    track_script {
        check_haproxy
    }

}
EOF
</code></pre>
<p>master2节点配置</p>
<pre><code>cat &gt; /etc/keepalived/keepalived.conf &lt;&lt;EOF 
! Configuration File for keepalived

global_defs {
   router_id k8s
}

vrrp_script check_haproxy {
    script &quot;killall -0 haproxy&quot;
    interval 3
    weight -2
    fall 10
    rise 2
}

vrrp_instance VI_1 {
    state BACKUP 
    interface ens33 
    virtual_router_id 51
    priority 200
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass ceb1b3ec013d66163d6ab
    }
    virtual_ipaddress {
        192.168.44.158
    }
    track_script {
        check_haproxy
    }

}
EOF
</code></pre>
<h3 id="33-启动和检查">3.3 启动和检查</h3>
<p>在两台master节点都执行</p>
<pre><code># 启动keepalived
$ systemctl start keepalived.service
设置开机启动
$ systemctl enable keepalived.service
# 查看启动状态
$ systemctl status keepalived.service
</code></pre>
<p>启动后查看master1的网卡信息</p>
<pre><code>ip a s ens33
</code></pre>
<h2 id="4-部署haproxy">4. 部署haproxy</h2>
<h3 id="41-安装">4.1 安装</h3>
<pre><code>yum install -y haproxy
</code></pre>
<h3 id="42-配置">4.2 配置</h3>
<p>两台master节点的配置均相同，配置中声明了后端代理的两个master节点服务器，指定了haproxy运行的端口为16443等，因此16443端口为集群的入口</p>
<pre><code>cat &gt; /etc/haproxy/haproxy.cfg &lt;&lt; EOF
#---------------------------------------------------------------------
# Global settings
#---------------------------------------------------------------------
global
    # to have these messages end up in /var/log/haproxy.log you will
    # need to:
    # 1) configure syslog to accept network log events.  This is done
    #    by adding the '-r' option to the SYSLOGD_OPTIONS in
    #    /etc/sysconfig/syslog
    # 2) configure local2 events to go to the /var/log/haproxy.log
    #   file. A line like the following can be added to
    #   /etc/sysconfig/syslog
    #
    #    local2.*                       /var/log/haproxy.log
    #
    log         127.0.0.1 local2
    
    chroot      /var/lib/haproxy
    pidfile     /var/run/haproxy.pid
    maxconn     4000
    user        haproxy
    group       haproxy
    daemon 
       
    # turn on stats unix socket
    stats socket /var/lib/haproxy/stats
#---------------------------------------------------------------------
# common defaults that all the 'listen' and 'backend' sections will
# use if not designated in their block
#---------------------------------------------------------------------  
defaults
    mode                    http
    log                     global
    option                  httplog
    option                  dontlognull
    option http-server-close
    option forwardfor       except 127.0.0.0/8
    option                  redispatch
    retries                 3
    timeout http-request    10s
    timeout queue           1m
    timeout connect         10s
    timeout client          1m
    timeout server          1m
    timeout http-keep-alive 10s
    timeout check           10s
    maxconn                 3000
#---------------------------------------------------------------------
# kubernetes apiserver frontend which proxys to the backends
#--------------------------------------------------------------------- 
frontend kubernetes-apiserver
    mode                 tcp
    bind                 *:16443
    option               tcplog
    default_backend      kubernetes-apiserver    
#---------------------------------------------------------------------
# round robin balancing between the various backends
#---------------------------------------------------------------------
backend kubernetes-apiserver
    mode        tcp
    balance     roundrobin
    server      master01.k8s.io   192.168.44.155:6443 check
    server      master02.k8s.io   192.168.44.156:6443 check
#---------------------------------------------------------------------
# collection haproxy statistics message
#---------------------------------------------------------------------
listen stats
    bind                 *:1080
    stats auth           admin:awesomePassword
    stats refresh        5s
    stats realm          HAProxy\ Statistics
    stats uri            /admin?stats
EOF
</code></pre>
<h3 id="43-启动和检查">4.3 启动和检查</h3>
<p>两台master都启动</p>
<pre><code># 设置开机启动
$ systemctl enable haproxy
# 开启haproxy
$ systemctl start haproxy
# 查看启动状态
$ systemctl status haproxy
</code></pre>
<p>检查端口</p>
<pre><code>netstat -lntup|grep haproxy
</code></pre>
<h2 id="5-所有节点安装dockerkubeadmkubelet">5. 所有节点安装Docker/kubeadm/kubelet</h2>
<p>Kubernetes默认CRI（容器运行时）为Docker，因此先安装Docker。</p>
<h3 id="51-安装docker">5.1 安装Docker</h3>
<pre><code>$ wget https://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo -O /etc/yum.repos.d/docker-ce.repo
$ yum -y install docker-ce-18.06.1.ce-3.el7
$ systemctl enable docker &amp;&amp; systemctl start docker
$ docker --version
Docker version 18.06.1-ce, build e68fc7a
</code></pre>
<pre><code>$ cat &gt; /etc/docker/daemon.json &lt;&lt; EOF
{
  &quot;registry-mirrors&quot;: [&quot;https://b9pmyelo.mirror.aliyuncs.com&quot;]
}
EOF
</code></pre>
<h3 id="52-添加阿里云yum软件源">5.2 添加阿里云YUM软件源</h3>
<pre><code>$ cat &gt; /etc/yum.repos.d/kubernetes.repo &lt;&lt; EOF
[kubernetes]
name=Kubernetes
baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64
enabled=1
gpgcheck=0
repo_gpgcheck=0
gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg
EOF
</code></pre>
<h3 id="53-安装kubeadmkubelet和kubectl">5.3 安装kubeadm，kubelet和kubectl</h3>
<p>由于版本更新频繁，这里指定版本号部署：</p>
<pre><code>$ yum install -y kubelet-1.16.3 kubeadm-1.16.3 kubectl-1.16.3
$ systemctl enable kubelet
</code></pre>
<h2 id="6-部署kubernetes-master">6. 部署Kubernetes Master</h2>
<h3 id="61-创建kubeadm配置文件">6.1 创建kubeadm配置文件</h3>
<p>在具有vip的master上操作，这里为master1</p>
<pre><code>$ mkdir /usr/local/kubernetes/manifests -p

$ cd /usr/local/kubernetes/manifests/

$ vi kubeadm-config.yaml

apiServer:
  certSANs:
    - master1
    - master2
    - master.k8s.io
    - 192.168.44.158
    - 192.168.44.155
    - 192.168.44.156
    - 127.0.0.1
  extraArgs:
    authorization-mode: Node,RBAC
  timeoutForControlPlane: 4m0s
apiVersion: kubeadm.k8s.io/v1beta1
certificatesDir: /etc/kubernetes/pki
clusterName: kubernetes
controlPlaneEndpoint: &quot;master.k8s.io:16443&quot;
controllerManager: {}
dns: 
  type: CoreDNS
etcd:
  local:    
    dataDir: /var/lib/etcd
imageRepository: registry.aliyuncs.com/google_containers
kind: ClusterConfiguration
kubernetesVersion: v1.16.3
networking: 
  dnsDomain: cluster.local  
  podSubnet: 10.244.0.0/16
  serviceSubnet: 10.1.0.0/16
scheduler: {}
</code></pre>
<h3 id="62-在master1节点执行">6.2 在master1节点执行</h3>
<pre><code>$ kubeadm init --config kubeadm-config.yaml
</code></pre>
<p>按照提示配置环境变量，使用kubectl工具：</p>
<pre><code class="language-bash">mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
$ kubectl get nodes
$ kubectl get pods -n kube-system
</code></pre>
<p><strong>按照提示保存以下内容，一会要使用：</strong></p>
<pre><code class="language-bash">kubeadm join master.k8s.io:16443 --token jv5z7n.3y1zi95p952y9p65 \
    --discovery-token-ca-cert-hash sha256:403bca185c2f3a4791685013499e7ce58f9848e2213e27194b75a2e3293d8812 \
    --control-plane 
</code></pre>
<p>查看集群状态</p>
<pre><code class="language-bash">kubectl get cs

kubectl get pods -n kube-system
</code></pre>
<h2 id="7安装集群网络">7.安装集群网络</h2>
<p>从官方地址获取到flannel的yaml，在master1上执行</p>
<pre><code class="language-bash">mkdir flannel
cd flannel
wget -c https://raw.githubusercontent.com/coreos/flannel/master/Documentation/kube-flannel.yml
</code></pre>
<p>安装flannel网络</p>
<pre><code class="language-bash">kubectl apply -f kube-flannel.yml 
</code></pre>
<p>检查</p>
<pre><code class="language-bash">kubectl get pods -n kube-system
</code></pre>
<h2 id="8-master2节点加入集群">8、master2节点加入集群</h2>
<h3 id="81-复制密钥及相关文件">8.1 复制密钥及相关文件</h3>
<p>从master1复制密钥及相关文件到master2</p>
<pre><code class="language-bash"># ssh root@192.168.44.156 mkdir -p /etc/kubernetes/pki/etcd

# scp /etc/kubernetes/admin.conf root@192.168.44.156:/etc/kubernetes
   
# scp /etc/kubernetes/pki/{ca.*,sa.*,front-proxy-ca.*} root@192.168.44.156:/etc/kubernetes/pki
   
# scp /etc/kubernetes/pki/etcd/ca.* root@192.168.44.156:/etc/kubernetes/pki/etcd
</code></pre>
<h3 id="82-master2加入集群">8.2 master2加入集群</h3>
<p>执行在master1上init后输出的join命令,需要带上参数<code>--control-plane</code>表示把master控制节点加入集群</p>
<pre><code>kubeadm join master.k8s.io:16443 --token ckf7bs.30576l0okocepg8b     --discovery-token-ca-cert-hash sha256:19afac8b11182f61073e254fb57b9f19ab4d798b70501036fc69ebef46094aba --control-plane
</code></pre>
<p>检查状态</p>
<pre><code>kubectl get node

kubectl get pods --all-namespaces
</code></pre>
<h2 id=""></h2>
<h2 id="5-加入kubernetes-node">5. 加入Kubernetes Node</h2>
<p>在node1上执行</p>
<p>向集群添加新节点，执行在kubeadm init输出的kubeadm join命令：</p>
<pre><code>kubeadm join master.k8s.io:16443 --token ckf7bs.30576l0okocepg8b     --discovery-token-ca-cert-hash sha256:19afac8b11182f61073e254fb57b9f19ab4d798b70501036fc69ebef46094aba
</code></pre>
<p><strong>集群网络重新安装，因为添加了新的node节点</strong></p>
<p>检查状态</p>
<pre><code>kubectl get node

kubectl get pods --all-namespaces
</code></pre>
<h2 id="-2"></h2>
<h2 id="7-测试kubernetes集群">7. 测试kubernetes集群</h2>
<p>在Kubernetes集群中创建一个pod，验证是否正常运行：</p>
<pre><code>$ kubectl create deployment nginx --image=nginx
$ kubectl expose deployment nginx --port=80 --type=NodePort
$ kubectl get pod,svc
</code></pre>
<p>访问地址：http://NodeIP:Port</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Kubernetes Ingress]]></title>
        <id>https://jwangkun.github.io/post/59eFp4SaS/</id>
        <link href="https://jwangkun.github.io/post/59eFp4SaS/">
        </link>
        <updated>2020-10-13T09:30:11.000Z</updated>
        <summary type="html"><![CDATA[<p>Ingress**也是Kubernetes项目里的一种 <strong>API</strong> 对象，它公开了从集群外部到集群内<code>Service</code>的 <strong>HTTP</strong> 和 <strong>HTTPS</strong> 路由，这些路由由 Ingress 资源上定义的规则控制。</p>
]]></summary>
        <content type="html"><![CDATA[<p>Ingress**也是Kubernetes项目里的一种 <strong>API</strong> 对象，它公开了从集群外部到集群内<code>Service</code>的 <strong>HTTP</strong> 和 <strong>HTTPS</strong> 路由，这些路由由 Ingress 资源上定义的规则控制。</p>
<!-- more -->
<pre><code>    internet
        |
   [ Ingress ]
   --|-----|--
   [ Services ]
</code></pre>
<p>如果用一句话概况<code>Ingress</code>的话就是：<code>Ingress</code>是<code>Service</code>们的反向代理。通过看<code>Ingress</code>对象的定义你会感觉自己在看<code>Nginx</code>的配置文件一样。</p>
<p><code>Ingress</code>资源对象的<code>YAML</code>定义。与大多数Kubernetes资源一样，也需要<code>apiVersion</code>，<code>Kind</code>，<code>Metadata</code>和<code>Spec</code> 这些组成部分。</p>
<p>一个典型的<code>Ingress</code>对象的定义如下所示：</p>
<pre><code>apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app-ingress
spec:
  rules:
    - host: app.example.com
      http:
        paths:
          - path: /
            backend:
              serviceName: app-service
              servicePort: 80
          - path: /v2
            backend:
              serviceName: app-service-v2
              servicePort: 80
</code></pre>
<p>在上面这个<code>Ingress</code>的<code>YAML</code>定义中，最值得我们关注的，是<strong>spec.rules</strong> 字段。在 Kubernetes 里，这个字段叫作：<strong>IngressRule</strong>。</p>
<p><code>IngressRule</code> 里面 <strong>host</strong> 字段定义的值，就是这个<code>Ingress</code>的入口。当访问 <code>app.example.com</code> 的时候，实际上访问到的是这个 <code>Ingress</code> 对象。这样就能使用 IngressRule 来对请求进行下一步转发。</p>
<p><code>IngressRule</code> 的定义，主要依赖于<code>path</code>字段。可以简单地理解为，这里的每一个<code>path</code>都对应一个后端 <code>Service</code>。上面的例子里，定义了两个<code>path</code>，它们分别对应：<strong>app-service</strong>和<strong>app-service-v2</strong> 两个后端<code>Service</code>。通过<code>Service</code>，流量最终会到达<code>Service</code>后面的<code>Pod</code>。</p>
<p><strong>servicePort</strong>字段指定的端口值就是在<code>Service</code>的定义里<code>port</code>字段的值，上一篇讲Service的文章里已经科普了一下<code>Service</code>定义里的<code>nodePort</code>，<code>port</code>和<code>targetPort</code>都是什么意思，感觉到懵圈（我自己都有点懵）的读者大大可以翻阅一下文章《<a href="https://mp.weixin.qq.com/s?__biz=MzUzNTY5MzU2MA==&amp;mid=2247486082&amp;idx=1&amp;sn=42a9bc8fcfc9da09445e9e2f4cf2fb96&amp;chksm=fa80db15cdf752039494992f71a3bc488cf386841bd1aaaa44115f5e7f155ba55ce468ec89ee&amp;token=1964476830&amp;lang=zh_CN&amp;scene=21#wechat_redirect">学练结合，快速掌握Kubernetes Service</a>》里的这部分内容。</p>
<p>所以 <code>Ingress</code> 对象，其实就是 <code>Kubernetes</code> 项目对**&quot;反向代理&quot;**的一种抽象。一个 <code>Ingress</code>对象的主要内容，实际上就是一个&quot;反向代理&quot;服务的配置文件的描述。而这个代理服务对应的转发规则，就是 <code>IngressRule</code>。这就是为什么在每条 <code>IngressRule</code> 里，需要有一个 <strong>host</strong> 字段来作为这条 <code>IngressRule</code> 的入口，然后还需要有一系列 <code>path</code> 字段来声明具体的转发策略。</p>
<p>有了<code>Ingress</code>后我们还需要<code>Ingress Controller</code>，它会根据你定义的<code>Ingress</code>对象，提供对应的代理能力。目前，业界常用的各种反向代理项目，比如 Nginx、Envoy 等，都已经为 Kubernetes 专门维护了对应的 Ingress Controller。</p>
<p>下面我就用最常用的Nginx Ingress Controller给这个系列教程一直以来用的Demo实践应用一下Ingress</p>
<h2 id="安装ingress-controller">安装Ingress Controller</h2>
<p>因为<code>Minikube</code>里边内置了Nginx Ingress Controller这个插件， 默认没有启用，所以如果是在<code>Minikube</code>这个单节点集群里实践的话只需要执行下面的命令：</p>
<pre><code>minikube addons enable ingress
</code></pre>
<p>检查验证 Nginx Ingress 控制器处于运行状态：</p>
<pre><code>kubectl get pods -n kube-system --filed-selector=Running
</code></pre>
<p>有下图红框里的<code>Pod</code>就证明已经安装成功了：</p>
<figure data-type="image" tabindex="1"><img src="https://mmbiz.qpic.cn/mmbiz_png/z4pQ0O5h0f7Cibmniccgicp2LOuOxgpXxicZ3QGjssI5jSX23qjj8BNPddYtehC9vonXtoL4nQUXYnF2LmOvaZwk7w/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<blockquote>
<p>因为这个Pod用的官方镜像是在红帽软件的镜像库里，所以安装时间可能会有点长，也可能会失败，如果网络条件允许的话可以在准备阶段先执行 docker pull http://quay.io/kubernetes-ingress-controller/nginx-ingress-controller:0.32.0 把镜像下载到本地。</p>
</blockquote>
<p>此外还有不少安装<code>Nginx Ingress Controller</code>的方式，比如用Kubernetes的包管理工具<strong>Helm</strong>安装，这些安装方式可以参考<strong>官方的部署指南</strong>[1]。</p>
<h2 id="创建ingress">创建Ingress</h2>
<p>因为我们之前给应用<code>Pod</code>创建的<code>Service</code>名字叫<strong>app-service</strong>，<code>port</code>字段指定的是端口80</p>
<pre><code>➜  ~ kubectl get svc
NAME          TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)        AGE
app-service   NodePort    10.108.26.155   &lt;none&gt;        80:30080/TCP   6d16h
kubernetes    ClusterIP   10.96.0.1       &lt;none&gt;        443/TCP        96d

➜  ~ kubectl describe svc app-service
Name:                     app-service
Namespace:                default
Labels:                   &lt;none&gt;
Annotations:              &lt;none&gt;
Selector:                 app=go-app
Type:                     NodePort
IP:                       10.108.26.155
Port:                     http  80/TCP
TargetPort:               3000/TCP
NodePort:                 http  30080/TCP
Endpoints:                10.1.0.24:3000,10.1.0.25:3000
</code></pre>
<p>这就确定了我们要创建建的<code>Ingress</code>对象，第一个<code>path</code> 里要设置的<strong>backend.serviceName</strong>和<strong>backend.servicePort</strong>字段的值，Ingress对象的<code>YAML</code>定义如下：</p>
<pre><code># app-ingress.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app-ingress
spec:
  rules:
    - host: app.example.com
      http:
        paths:
          - path: /
            backend:
              serviceName: app-service
              servicePort: 80
</code></pre>
<blockquote>
<p>说明：因为目前为止我们只创建了一个Service，所以在Ingress里也只能设置一个path。</p>
</blockquote>
<p>这个Ingress定义里设置的<code>IngressRules</code>是把所有对app.example.com入口的请求都路由到<code>app-service</code>这个<code>Service</code>的80端口。</p>
<p>定义好<code>Ingress</code>对象后，也是执行<strong>kubectl apply -f</strong>创建对象，可以看到所有的对象的创建和更新都可以用<strong>apply</strong>操作搞定，这就是<code>Kubernetes</code>项目声明式定义的好处。</p>
<pre><code>kubectl apply -f app-ingress.yaml --record

# 执行后的输出
ingress.networking.k8s.io/app-ingress created
</code></pre>
<h2 id="验证ingress">验证Ingress</h2>
<p>查询Ingress是否创建成功，使用通用的<strong>kubectl get</strong>命令：</p>
<pre><code>➜  ~ kubectl get ingress
NAME          CLASS    HOSTS             ADDRESS        PORTS   AGE
app-ingress   &lt;none&gt;   app.example.com   192.168.64.4   80      20s
</code></pre>
<blockquote>
<p>有可能需要在提交创建操作几分钟后才能在集群里查询到Ingress</p>
</blockquote>
<p>在集群里查询到Ingress后，就可以通过<strong>kubctl describe ingress</strong>命令查看<code>Ingress</code>对象是否按照我们的定义成功代理了<code>app-service</code>这个<code>Service</code>：</p>
<pre><code>➜  ~ kubectl describe ingress app-ingress
Name:             app-ingress
Namespace:        default
Address:          192.168.64.4
Default backend:  default-http-backend:80 (&lt;error: endpoints &quot;default-http-backend&quot; not found&gt;)
Rules:
  Host             Path  Backends
  ----             ----  --------
  app.example.com
                   /   app-service:80 (10.1.0.24:3000,10.1.0.25:3000)
Annotations:       kubernetes.io/change-cause: kubectl apply --filename=app-ingress.yaml --record=true
Events:            &lt;none&gt;
</code></pre>
<p>上面输出里的<code>Rlues</code>部分可以清楚的看到，把<strong>Host:  app.example.com</strong>所有请求（定义了Path是/）都代理到了后端<code>app-service</code>的80端口，Service后面的<code>Pod</code>正是它的<code>Endpoints</code>，与上面的<strong>kubctl describe svc app-service</strong>命令输出里的<code>Endpoints</code>信息一致。</p>
<p>接下来在<code>/etc/hosts</code>文件里追加下面的内容，就能通过域名访问我们的服务了：</p>
<pre><code>192.168.64.4 app.example.com
</code></pre>
<p>你们在练习的时候，可以自己尝试新增一个<code>Service</code>，然后更新Ingress，再指定一个<code>/v2</code>之类的Path，让所有匹配这个规则的请求都能路由给新的Service。</p>
<h2 id="接下来">接下来</h2>
<p><code>Ingress</code>还有很多其他的配置，想要简单的讲完，还是挺难的。最常用的比如怎么设置TLS私钥和证书这些配置在<strong>Kubernetes官方文档-Ingress</strong>[2] 部分都有提到，后面自己练习的时候可以试试给<code>Ingress</code>启用<code>HTTPs</code>访问的功能。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ Kubernetes 集群升级指南：从理论到实践]]></title>
        <id>https://jwangkun.github.io/post/NDmieJqbU/</id>
        <link href="https://jwangkun.github.io/post/NDmieJqbU/">
        </link>
        <updated>2020-10-13T09:23:39.000Z</updated>
        <summary type="html"><![CDATA[<p>原创 高相林（禅鸣） [阿里巴巴云原生](javascript:void(0)😉</p>
<figure data-type="image" tabindex="1"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/yvBJb5IiafvnMFaOoAklrxdOvPwLJwOS8Gg0dtQYSV3z6IwqBe4BFlSdyvFyUZBCVxMBIpZaZd6To5AZcvg3Acg/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="" loading="lazy"></figure>
<p>作者 | 高相林（禅鸣）</p>
<p>**导读：**集群升级是 Kubernetes 集群生命周期中最为重要的一环，也是众多使用者最为谨慎对待的操作之一。为了更好地理解集群升级这件事情的内涵外延，我们首先会对集群升级的必要性和难点进行阐述；随后会对集群升级前必须要做的前置检查进行逐一讲解；接下来会对两种常见的升级方式进行展开介绍；最后对集群升级的三个步骤进行讲解，帮助读者从理论走入实践。<img src="https://mmbiz.qpic.cn/mmbiz_gif/US10Gcd0tQGY9ddd5GpbmVRuaRfuaESAUBGE7uHX5G0nxxLSub2QTKZdu538V7GaHXS5jsTCebYCUibaHsjg0ow/640?wx_fmt=gif&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1" alt="动态黑色音符" loading="lazy"></p>
]]></summary>
        <content type="html"><![CDATA[<p>原创 高相林（禅鸣） [阿里巴巴云原生](javascript:void(0)😉</p>
<figure data-type="image" tabindex="1"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/yvBJb5IiafvnMFaOoAklrxdOvPwLJwOS8Gg0dtQYSV3z6IwqBe4BFlSdyvFyUZBCVxMBIpZaZd6To5AZcvg3Acg/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="" loading="lazy"></figure>
<p>作者 | 高相林（禅鸣）</p>
<p>**导读：**集群升级是 Kubernetes 集群生命周期中最为重要的一环，也是众多使用者最为谨慎对待的操作之一。为了更好地理解集群升级这件事情的内涵外延，我们首先会对集群升级的必要性和难点进行阐述；随后会对集群升级前必须要做的前置检查进行逐一讲解；接下来会对两种常见的升级方式进行展开介绍；最后对集群升级的三个步骤进行讲解，帮助读者从理论走入实践。<img src="https://mmbiz.qpic.cn/mmbiz_gif/US10Gcd0tQGY9ddd5GpbmVRuaRfuaESAUBGE7uHX5G0nxxLSub2QTKZdu538V7GaHXS5jsTCebYCUibaHsjg0ow/640?wx_fmt=gif&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1" alt="动态黑色音符" loading="lazy"></p>
<!-- more -->
<p><strong>升级的必要性&amp;难点</strong></p>
<p>在 Kubernetes 领域，得益于活跃的开源社区，Kubernetes 的迭代速度较快，目前保持在每个季度发行一个新版本的节奏。新版本的 Kubernetes 有着更为先进的新特性、更加全面的安全加固和漏洞修复。前一段时间社区刚刚完成了 1.19 版本的正式发布。</p>
<p>对于发展如此快速的开源项目，跟上社区的步伐就显得更为重要，而集群升级能力就是帮助我们跟上社区步伐的不二选择。我们可以从以下两个方面对集群升级的必要性进行说明：</p>
<ul>
<li>
<p><strong>对于 Kubernetes 集群的使用者</strong>：更新的 Kubernetes 版本意味着更新的 feature，更加全面的安全补丁，和诸多的 bugfix。我们可以通过集群升级功能充分享受活跃的 Kubernetes 开源社区带来的发展红利；</p>
</li>
<li>
<p><strong>对于 Kubernetes 集群的运维者</strong>：通过集群升级功能可以拉齐所管理的集群版本，减少集群版本的碎片化，从而减少 Kubernetes 版本碎片化带来的管理成本和维护成本。</p>
</li>
</ul>
<p>讲完了集群升级的必要性，我们来详细看一下集群升级的难点。</p>
<p>目前多数 Kubernetes 使用者对集群升级这件事持有着非常保守的态度，害怕集群在升级的过程中出现不可预期的情况，也有使用者将集群升级称之为“给飞行中的飞机换引擎”。那么，使用者对于升级的保守态度主要来源于什么原因呢？我认为有以下几点：</p>
<ul>
<li>
<p>经过长时间的运行后，Kubernetes 集群已经累计了复杂的运行时状态；</p>
</li>
<li>
<p>Kubernetes 集群运维者会根据集群承载的不同业务，对集群进行不同的配置，从而导致每个集群都有自己的差异化配置，可能会造成“千集群千面”；</p>
</li>
<li>
<p>对于云上运行的 Kubernetes 集群来说，其使用了大量的云计算底层资源。众多的底层云资源就会带来众多的不确定性因素。</p>
</li>
</ul>
<p>“千集群千面”的情况的存在，导致了集群升级需要以一套逻辑完成各种不同情况集群的升级工作，这也正是集群升级的困难之处。</p>
<figure data-type="image" tabindex="2"><img src="https://mmbiz.qpic.cn/mmbiz_gif/US10Gcd0tQGY9ddd5GpbmVRuaRfuaESAUBGE7uHX5G0nxxLSub2QTKZdu538V7GaHXS5jsTCebYCUibaHsjg0ow/640?wx_fmt=gif&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1" alt="动态黑色音符" loading="lazy"></figure>
<p><strong>升级预检</strong></p>
<p>正如我们前面所说，给正在对外提供服务的 Kubernetes 集群升级，就好比是“给飞行中的飞机换引擎”。因为集群升级面临着众多难点，也使得众多的 Kubernetes 集群维护者对集群升级这件事情比较紧张。</p>
<p>我们可以通过详细的升级预检，来消除集群升级的不确定性。对于上面列举的集群升级的难点，我们也可以分别进行详细的升级预检，对症下药，将难点逐一击破。升级预检主要可以分为三个方面：</p>
<ul>
<li>核心组件健康检查</li>
<li>节点配置检查</li>
<li>云资源检查</li>
</ul>
<h3 id="1-核心组件健康检查"><strong>1. 核心组件健康检查</strong></h3>
<p>说到核心组件健康检查，就不得不剖析一下集群的健康对于集群升级的重要性。一个不健康的集群很可能会在升级中出现各种异常的问题，就算侥幸完成了升级，各种问题也会在后续使用中逐渐凸显出来。</p>
<p>有人会说，我的集群看起来挺健康的，但是升级之后就出现问题了。一般来说，之所以会发生这种情况，是因为在集群在升级之前，这个问题已经存在了，只不过是在经历了集群升级之后才显现出来。</p>
<p>在了解了核心组件健康检查的必要性之后，我们来看一下都需要对那些组件进行检查：</p>
<ul>
<li>网络组件：需要确保网络组件版本和需要升级到的目标 Kubernetes 版本兼容；</li>
<li>apiservice：需要确保集群内的 apiservice 都可用；</li>
<li>节点：需要确定节点全部健康。</li>
</ul>
<h3 id="2-节点配置检查"><strong>2. 节点配置检查</strong></h3>
<p>节点作为承载 Kubernetes 的底层元计算资源，不仅运行着 Kubelet、Docker 等重要的系统进程，也充当着集群和底层硬件交互接口的角色。</p>
<p>确保节点的健康性和配置的正确性是确保整个集群健康性重要的一环。下面就对所需的检查项进行讲解。</p>
<ul>
<li>操作系统配置：需要确定基础的系统组件（yum、systemd 和 ntp 等系统服务是否正常）和内核参数是否配置合理；</li>
<li>kubelet：需要确定 kubelet 的进程健康、配置正确；</li>
<li>Docker：需要确定 Docker 的进程健康、配置正确。</li>
</ul>
<h3 id=""></h3>
<h3 id="3-云资源检查"><strong>3. 云资源检查</strong></h3>
<p>运行在云上的 Kubernetes 集群依赖着众多云资源，一旦集群所依赖的云资源不健康或者配置错误，就会影响到整个集群的正常运行。我们主要对下列云资源的状态和配置进行预检：</p>
<ul>
<li>apiserver 所使用的 SLB：需要确定实例的健康状态和端口配置（转发配置和访问控制配置等）；</li>
<li>集群所使用的 VPC 和 VSwitch：需要确定实例的健康状况；</li>
<li>集群内的 ECS 实例：需要确定其健康状况和网络配置。</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://mmbiz.qpic.cn/mmbiz_gif/US10Gcd0tQGY9ddd5GpbmVRuaRfuaESAUBGE7uHX5G0nxxLSub2QTKZdu538V7GaHXS5jsTCebYCUibaHsjg0ow/640?wx_fmt=gif&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1" alt="动态黑色音符" loading="lazy"></figure>
<p><strong>两种常见的升级方式</strong></p>
<p>在软件升级领域，有两种主流的软件升级方式，即原地升级和替换升级。这两种升级方式同样适用于 Kubernetes 集群，它们采用了不同软件升级思路，但也都存在着各自的利弊。下面我们来对这两种集群升级方式进行逐一讲解。</p>
<h3 id="-2"></h3>
<h3 id="1-原地升级"><strong>1. 原地升级</strong></h3>
<p>原地升级是一种精细化的、对这个那个集群改动量相对较小的一种升级方式。在升级容器的 worker 节点时，该升级方式会通过在 ECS 上原地替换 Kubernetes 组件的方式（主要为 kubelet 和其相关组件），完成整个集群的升级工作。阿里云容器服务 Kubernetes 为客户提供的集群升级就是基于这种方式的。</p>
<p>以将 Kubernetes 的版本从 1.14 升级到 1.16 为例。首先我们会对 ECS A 上的原本为 1.14 的 Kubelet 及其配置升级为 1.16，在完成节点 ECS A 上的组件升级之后，该节点也就被成功的升级到了 1.16。然后我们再对 ECS B 进行相同的操作，将其升级为 1.16，从而完成整个集群的升级工作。</p>
<p>在这个过程中节点保持运行，ECS 的相关配置也不会被修改。如图所示：</p>
<figure data-type="image" tabindex="4"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/yvBJb5IiafvnMFaOoAklrxdOvPwLJwOS8qiaHeIqCFuZ3r7bYiat3G0f29ODXwLd26fLKIYDZPrnsbTXib36OkOPHg/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="image.png" loading="lazy"></figure>
<h3 id="1优点"><strong>1）优点</strong></h3>
<ul>
<li>
<p>原地升级通过原地替换 kubelet 组件的方式对节点进行版本升级，从而保证了节点上的 Pod 不会因为集群升级而重建，确保了业务的连贯性；</p>
</li>
<li>
<p>该种升级方式不会对底层 ECS 本身进行修改和替换，保证了依赖特定节点调度的业务可以正常运行，也对 ECS 的包年包月客户更加友好。</p>
</li>
</ul>
<h3 id="2缺点"><strong>2）缺点</strong></h3>
<ul>
<li>
<p>原地升级方式需要在节点上进行一系列升级操作，才能完成整个节点的升级工作，这就导致整个升级过程不够原子化，可能会在中间的某一步骤失败，从而导致该节点升级失败；</p>
</li>
<li>
<p>原地升级的另一个缺点是需要预留一定量的资源，只有在资源足够的情况下升级程序才能在 ECS 上完成对节点的升级。</p>
</li>
</ul>
<h3 id="-3"></h3>
<h3 id="2-替换升级"><strong>2. 替换升级</strong></h3>
<p>替换升级又称轮转升级，相对于原地升级，替换升级是一种更加粗狂和原子化的升级方式。替换升级会逐个将旧版本的节点从集群中移除，并用新版本全新的节点来替换，从而完成整个 Kubernetes 集群的升级工作。</p>
<p>同样以将 Kubernetes 的版本从 1.14 升级到 1.16 为例。使用替代轮转方式的情况下，我们会将集群中 1.14 版本的节点依次进行排水并从集群中移除，并直接加入 1.16 版本的节点。即将 1.14 节点的 ECS A 从节点剔除，并将 1.16 节点的 ECS C 加入集群，再将 ECS B 从集群中删除，最后将 ECS D 加入到集群中。</p>
<p>这样就完成了所有节点的轮转工作，整个集群就也就升级到 1.16 了。如图所示：</p>
<figure data-type="image" tabindex="5"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/yvBJb5IiafvnMFaOoAklrxdOvPwLJwOS8zlkoL6tgFcG4pLUpfoS1icC49PZRgGbprf1L64tWULyAFcT18oic29ow/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="image.png" loading="lazy"></figure>
<h3 id="1优点-2"><strong>1）优点</strong></h3>
<p>替代升级通过将旧版本的节点替换为新版本的节点从而完成集群升级。这个替换的过程相较于原地升级更为原子化，也不存在那么复杂的中间状态，所以也不需要在升级之前进行太多的前置检查。相对应地，升级过程中可能会出现的各种稀奇古怪的问题也会减少很多。</p>
<h3 id="2缺点-2"><strong>2）缺点</strong></h3>
<ul>
<li>
<p>替代升级将集群内的节点全部进行替换和重置，所有节点都会经历排水的过程，这就会使集群内的 pod 进行大量迁移重建，对于 pod 重建容忍度比较低的业务、只有单副本的应用、stateful set 等相关应用不够友好，可能会因此发生不可用或者故障；</p>
</li>
<li>
<p>所有的节点经历重置，储存在节点本地磁盘上的数据都会丢失；</p>
</li>
<li>
<p>这种升级方式可能会带来宿主机 IP 变化等问题，对包年包月用户也不够友好。</p>
</li>
</ul>
<h2 id="-4"></h2>
<figure data-type="image" tabindex="6"><img src="https://mmbiz.qpic.cn/mmbiz_gif/US10Gcd0tQGY9ddd5GpbmVRuaRfuaESAUBGE7uHX5G0nxxLSub2QTKZdu538V7GaHXS5jsTCebYCUibaHsjg0ow/640?wx_fmt=gif&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1" alt="动态黑色音符" loading="lazy"></figure>
<p><strong>集群升级三部曲</strong></p>
<p>一个 Kubernetes 集群主要由负责集群管控的 master，执行工作负载的 worker 和众多功能性的系统组件组成。对一个 Kubernetes 集群升级，也就是对集群中的这三个部分进行分别升级。</p>
<p>故集群升级的三部曲为：</p>
<ul>
<li>滚动升级 master</li>
<li>分批升级 worker</li>
<li>系统组件升级（主要为 CoreDNS，kube-proxy 等核心组件）</li>
</ul>
<figure data-type="image" tabindex="7"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/yvBJb5IiafvnMFaOoAklrxdOvPwLJwOS8XdjExtDy2zPSuMBWibKkJiamWeIR3EWY7otC99pkFyVG7wuevsRHp9NA/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="image.png" loading="lazy"></figure>
<p>下面我们来对集群升级三部曲进行详细的讲解。</p>
<h3 id="1-滚动升级-master"><strong>1. 滚动升级 master</strong></h3>
<p>master 作为集群的大脑，承担了与使用者交互、任务调度和各种功能性的任务处理。集群 master 的部署方式也比较多样，可以通过 static pod 进行部署，可以通过本地进程进行部署，也可以通过 Kubernetes on Kubernetes 的方式在另一个集群内通过 pod 的方式部署。</p>
<p>总而言之，无论 master 为哪种部署方式，想要升级 master 主要就是对 master 中的三大件进行版本升级，主要包括：</p>
<ul>
<li>升级 kube-apiserver</li>
<li>升级 kube-controller-manager</li>
<li>升级 kube-scheduler</li>
</ul>
<p>需要注意，为了保证 Kubernetes apiserver 的可用性不中断，master 中的 kube-apiserver 最少需要有两个，这样才可以实现滚动升级，从而保证 apiserver 不会出现 downtime。</p>
<h3 id="2-分批升级-worker"><strong>2. 分批升级 worker</strong></h3>
<p>在完成 master 的升级工作之后，我们才可以开始对 worker 进行升级。Worker 升级主要是对节点上的 kubelet 及其依赖组件（如 cni 等）进行升级。为了保证集群中 worker 不会同时发生大批量的 kubelet 重启，所以我们需要对 worker 节点进行分批升级。</p>
<p>需要注意，我们必须要先升级 master，再升级 worker。因为高版本的 kubelet 在连接低版本的 master 时，很可能会出现不兼容的情况，从而导致节点 not ready。对于低版本的 kubelet 连接高版本的 apiserver，开源社区保证了 apiserver 对于 kubelet 两个版本的向后兼容性，即 1.14 的 kubelet 可以连接到 1.16 的 apiserver，而不会发生兼容性问题。</p>
<h3 id="-5"></h3>
<h3 id="3-核心系统组件升级"><strong>3. 核心系统组件升级</strong></h3>
<p>为了保证集群中各个组件的兼容性，我们需要在升级集群的同时对集群中的核心系统组件进行同步升级，主要包括：</p>
<ul>
<li><strong>Dns 组件</strong>：根据社区的版本兼容矩阵，将 CoreDNS 的版本升级为与集群版本相对应的版本；</li>
</ul>
<p><strong>社区的版本兼容矩阵</strong>：https://github.com/coredns/deployment/blob/master/kubernetes/CoreDNS-k8s_version.md</p>
<ul>
<li><strong>网络转发组件</strong>：Kube-proxy 的版本是跟随 Kubernetes 的版本进行演进的，所有我们需要将 kube-proxy 的版本升级到与 Kubernetes 版本相同的版本。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Kubernetes 网络插件工作原理]]></title>
        <id>https://jwangkun.github.io/post/IC6DrTsc6/</id>
        <link href="https://jwangkun.github.io/post/IC6DrTsc6/">
        </link>
        <updated>2020-10-13T09:17:09.000Z</updated>
        <summary type="html"><![CDATA[<figure data-type="image" tabindex="1"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3n5sBnyHgeuQBiaFVjeiaiaX67XW1V86TLWiaDUsiaXicqjMG1poRUGPy6vFQ/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>容器的网络解决方案有很多种，每支持一种网络实现就进行一次适配显然是不现实的，而 CNI 就是为了兼容多种网络方案而发明的。CNI 是 Container Network Interface 的缩写，是一个标准的通用的接口，用于连接容器管理系统和网络插件。</p>
]]></summary>
        <content type="html"><![CDATA[<figure data-type="image" tabindex="1"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3n5sBnyHgeuQBiaFVjeiaiaX67XW1V86TLWiaDUsiaXicqjMG1poRUGPy6vFQ/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>容器的网络解决方案有很多种，每支持一种网络实现就进行一次适配显然是不现实的，而 CNI 就是为了兼容多种网络方案而发明的。CNI 是 Container Network Interface 的缩写，是一个标准的通用的接口，用于连接容器管理系统和网络插件。</p>
<!-- more -->
<p>简单来说，容器 runtime 为容器提供 network namespace，网络插件负责将 network interface 插入该 network namespace 中并且在宿主机做一些必要的配置，最后对 namespace 中的 interface 进行 IP 和路由的配置。</p>
<p>所以网络插件的主要工作就在于为容器提供网络环境，包括为 pod 设置 ip 地址、配置路由保证集群内网络的通畅。目前比较流行的网络插件是 Flannel 和 Calico。</p>
<h2 id="flannel">Flannel</h2>
<p>Flannel 主要提供的是集群内的 Overlay 网络，支持三种后端实现，分别是：UDP 模式、VXLAN 模式、host-gw 模式。</p>
<h3 id="udp-模式">UDP 模式</h3>
<p>UDP 模式，是 Flannel 项目最早支持的一种方式，却也是性能最差的一种方式。这种模式提供的是一个三层的 Overlay 网络，即：它首先对发出端的 IP 包进行 UDP 封装，然后在接收端进行解封装拿到原始的 IP 包，进而把这个 IP 包转发给目标容器。工作原理如下图所示。</p>
<figure data-type="image" tabindex="2"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3Tpz3pBw5DuxQaSdlP67EbwNtJVKKc20UhdGVA3ib2zFzc3XvtOGEicPw/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>node1 上的 pod1 请求 node2 上的 pod2 时，流量的走向如下：</p>
<ol>
<li>pod1 里的进程发起请求，发出 IP 包；</li>
<li>IP 包根据 pod1 里的 veth 设备对，进入到 cni0 网桥；</li>
<li>由于 IP 包的目的 ip 不在 node1 上，根据 flannel 在节点上创建出来的路由规则，进入到 flannel0 中；</li>
<li>此时 flanneld 进程会收到这个包，flanneld 判断该包应该在哪台 node 上，然后将其封装在一个 UDP 包中；</li>
<li>最后通过 node1 上的网关，发送给 node2；</li>
</ol>
<p>flannel0 是一个 TUN 设备（Tunnel 设备）。在 Linux 中，TUN 设备是一种工作在三层（Network Layer）的虚拟网络设备。TUN 设备的功能：在操作系统内核和用户应用程序之间传递 IP 包。</p>
<p>可以看到，这种模式性能差的原因在于，整个包的 UDP 封装过程是 flanneld 程序做的，也就是用户态，而这就带来了一次内核态向用户态的转换，以及一次用户态向内核态的转换。在上下文切换和用户态操作的代价其实是比较高的，而 UDP 模式因为封包拆包带来了额外的性能消耗。</p>
<h3 id="vxlan-模式">VXLAN 模式</h3>
<p>VXLAN，即 Virtual Extensible LAN（虚拟可扩展局域网），是 Linux 内核本身就支持的一种网络虚拟化技术。通过利用 Linux 内核的这种特性，也可以实现在内核态的封装和解封装的能力，从而构建出覆盖网络。其工作原理如下图所示：</p>
<figure data-type="image" tabindex="3"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3EVTNRxTO4tWRQVjEfUW39O4ACIjhj0xSTW5nSblDR0PRjZa8tbU0lQ/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>VXLAN 模式的 flannel 会在节点上创建一个叫 flannel.1 的 VTEP (VXLAN Tunnel End Point，虚拟隧道端点) 设备，跟 UDP 模式一样，该设备将二层数据帧封装在 UDP 包里，再转发出去，而与 UDP 模式不一样的是，整个封装的过程是在内核态完成的。</p>
<p>node1 上的 pod1 请求 node2 上的 pod2 时，流量的走向如下：</p>
<ol>
<li>pod1 里的进程发起请求，发出 IP 包；</li>
<li>IP 包根据 pod1 里的 veth 设备对，进入到 cni0 网桥；</li>
<li>由于 IP 包的目的 ip 不在 node1 上，根据 flannel 在节点上创建出来的路由规则，进入到 flannel.1 中；</li>
<li>flannel.1 将原始 IP 包加上一个目的 MAC 地址，封装成一个二层数据帧；然后内核将数据帧封装进一个 UDP 包里；</li>
<li>最后通过 node1 上的网关，发送给 node2；</li>
</ol>
<h3 id="抓包验证">抓包验证</h3>
<p>在 node1 上部署一个 nginx pod1，node2 上部署一个 nginx pod2。然后在 pod1 的容器中 curl pod2 容器的 80 端口。</p>
<p>集群网络环境如下：</p>
<pre><code>node1 网卡 ens33：192.168.50.10pod1 veth：10.244.0.7node1 cni0：10.244.0.1/24node1 flannel.1：10.244.0.0/32
node2 网卡 ens33：192.168.50.11pod2 veth：10.244.1.9node2 cni0：10.244.1.1/24node2 flannel.1：10.244.1.0/32
</code></pre>
<p>node1 上的路由信息如下：</p>
<pre><code>➜  ~ ip routedefault via 192.168.50.1 dev ens3310.244.0.0/24 dev cni0 proto kernel scope link src 10.244.0.110.244.1.0/24 via 10.244.1.0 dev flannel.1 onlink10.244.2.0/24 via 10.244.2.0 dev flannel.1 onlink172.17.0.0/16 dev docker0 proto kernel scope link src 172.17.0.1192.168.50.0/24 dev ens33 proto kernel scope link src 192.168.50.10 metric 100
</code></pre>
<p>node1 的网卡 ens33 的抓包情况：<img src="https://mmbiz.qpic.cn/mmbiz_png/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs32qSqlWzNGUNomJIbtxy2xz1nAAOmtsF0nvCNopRK2K5hCoBIBH6iaKQ/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></p>
<p>只能看到源 ip 为 node1 ip、目的 ip 为 node2 ip 的 UDP 包。由于 flannel.1 进行了一层 UDP 封包，这里我们在 Wireshark 中设置一下将 UDP 包解析为 VxLAN 格式（端口为 8472），设置过程为 Analyze-&gt;Decode As：</p>
<figure data-type="image" tabindex="4"><img src="https://mmbiz.qpic.cn/mmbiz_png/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3DGjyajXV7pWPl6UXgSRpl6fQcECicW6xCPrlVQBSicl9INmZNa9I8CuA/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>然后再来看一下 node1 网卡上收到的包：<img src="https://mmbiz.qpic.cn/mmbiz_png/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3Oe1uicleyT2O7qibTJuMd2SH2Fl9H09pKlLRnEwx99pRg2U841XfHgrw/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></p>
<p>可以看到源 ip 为 pod1 ip、目的 ip 为 pod2 ip，并且该 IP 包被封装在 UDP 包中。</p>
<h3 id="host-gw-模式">host-gw 模式</h3>
<p>最后一种 host-gw 模式是一种纯三层网络方案。其工作原理为将每个 Flannel 子网的“下一跳”设置成了该子网对应的宿主机的 IP 地址，这台主机会充当这条容器通信路径里的“网关”。这样 IP 包就能通过二层网络达到目的主机，而正是因为这一点，host-gw 模式要求集群宿主机之间的网络是二层连通的，如下图所示。</p>
<figure data-type="image" tabindex="5"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3ecIHqptBxFLPV3Q0wkicmkV7ZzuiaXU6Jjdybqyr64RMiaX1pKkRzZsicg/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>宿主机上的路由信息是 flanneld 设置的，因为 flannel 子网和主机的信息保存在 etcd 中，所以 flanneld 只需要 watch 这些数据的变化，实时更新路由表即可。在这种模式下，容器通信的过程就免除了额外的封包和解包带来的性能损耗。</p>
<p>node1 上的 pod1 请求 node2 上的 pod2 时，流量的走向如下：</p>
<ol>
<li>pod1 里的进程发起请求，发出 IP 包，从网络层进入链路层封装成帧；</li>
<li>根据主机上的路由规则，数据帧从 Node 1 通过宿主机的二层网络到达 Node 2 上；</li>
</ol>
<h2 id="calico">Calico</h2>
<p>Calico 没有使用 CNI 的网桥模式，而是将节点当成边界路由器，组成了一个全连通的网络，通过 BGP 协议交换路由。所以，Calico 的 CNI 插件还需要在宿主机上为每个容器的 Veth Pair 设备配置一条路由规则，用于接收传入的 IP 包。</p>
<p>Calico 的组件：</p>
<ol>
<li>CNI 插件：Calico 与 Kubernetes 对接的部分</li>
<li>Felix：负责在宿主机上插入路由规则（即：写入 Linux 内核的 FIB 转发信息库），以及维护 Calico 所需的网络设备等工作。</li>
<li>BIRD (BGP Route Reflector)：是 BGP 的客户端，专门负责在集群里分发路由规则信息。</li>
</ol>
<p>三个组件都是通过一个 DaemonSet 安装的。CNI 插件是通过 initContainer 安装的；而 Felix 和 BIRD 是同一个 pod 的两个 container。</p>
<h3 id="工作原理">工作原理</h3>
<p>Calico 采用的 BGP，就是在大规模网络中实现节点路由信息共享的一种协议。全称是 Border Gateway Protocol，即：边界网关协议。它是一个 Linux 内核原生就支持的、专门用在大规模数据中心里维护不同的 “自治系统” 之间路由信息的、无中心的路由协议。</p>
<p>由于没有使用 CNI 的网桥，Calico 的 CNI 插件需要为每个容器设置一个 Veth Pair 设备，然后把其中的一端放置在宿主机上，还需要在宿主机上为每个容器的 Veth Pair 设备配置一条路由规则，用于接收传入的 IP 包。如下图所示：</p>
<figure data-type="image" tabindex="6"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3txgkibeQ3fJS3QgvIUPYwXiamwb8wVITibkBnWibHnaFkibxbQdlseNhnGg/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>可以使用 calicoctl 查看 node1 的节点连接情况：</p>
<pre><code>~ calicoctl get noNAMEnode1node2node3~ calicoctl node statusCalico process is running.
IPv4 BGP status+---------------+-------------------+-------+------------+-------------+| PEER ADDRESS  |     PEER TYPE     | STATE |   SINCE    |    INFO     |+---------------+-------------------+-------+------------+-------------+| 192.168.50.11 | node-to-node mesh | up    | 2020-09-28 | Established || 192.168.50.12 | node-to-node mesh | up    | 2020-09-28 | Established |+---------------+-------------------+-------+------------+-------------+
IPv6 BGP statusNo IPv6 peers found.
</code></pre>
<p>可以看到整个 calico 集群上有 3 个节点，node1 和另外两个节点处于连接状态，模式为 “Node-to-Node Mesh”。再看下 node1 上的路由信息如下：</p>
<pre><code>~ ip routedefault via 192.168.50.1 dev ens33 proto static metric 10010.244.104.0/26 via 192.168.50.11 dev ens33 proto bird10.244.135.0/26 via 192.168.50.12 dev ens33 proto bird10.244.166.128 dev cali717821d73f3 scope linkblackhole 10.244.166.128/26 proto bird172.17.0.0/16 dev docker0 proto kernel scope link src 172.17.0.1192.168.50.0/24 dev ens33 proto kernel scope link src 192.168.50.10 metric 100
</code></pre>
<p>其中，第 2 条的路由规则表明 10.244.104.0/26 网段的数据包通过 bird 协议由 ens33 设备发往网关 192.168.50.11。这也就定义了目的 ip 为 node2 上 pod 请求的走向。第 3 条路由规则与之类似。</p>
<h4 id="抓包验证-2">抓包验证</h4>
<p>与上面一样，从 node1 上的 pod1 发送一个 http 请求到 node2 上的 pod2。</p>
<p>集群网络环境如下：</p>
<pre><code>node1 网卡 ens33：192.168.50.10pod1 ip：10.244.166.128
node2 网卡 ens33：192.168.50.11pod2 ip：10.244.104.1
</code></pre>
<p>node1 的网卡 ens33 的抓包情况：</p>
<figure data-type="image" tabindex="7"><img src="https://mmbiz.qpic.cn/mmbiz_png/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3QPUAWQwDmsZOiaMqkbSxIkNUABGTQHrhRYLGicsWgJAcAkHNnYoCZCTQ/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<h3 id="ipip-模式">IPIP 模式</h3>
<p>IPIP 模式为了解决两个 node 不在一个子网的问题。只要将名为 calico-node 的 daemonset 的环境变量 CALICOIPV4POOLIPIP 设置为 &quot;Always&quot; 即可。如下：</p>
<pre><code>- name: CALICO_IPV4POOL_IPIP  value: &quot;Off&quot;
</code></pre>
<p>IPIP 模式的 calico 使用了 tunl0 设备，这是一个 IP 隧道设备。IP 包进入 tunl0 后，内核会将原始 IP 包直接封装在宿主机的 IP 包中；封装后的 IP 包的目的地址为下一跳地址，即 node2 的 IP 地址。由于宿主机之间已经使用路由器配置了三层转发，所以这个 IP 包在离开 node 1 之后，就可以经过路由器，最终发送到 node 2 上。如下图所示。</p>
<figure data-type="image" tabindex="8"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs34yic0D0kkFvhytZic1HtNMKM1o1PUM2IxVnROHAA7k0gygfbCosNO7MQ/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>由于 IPIP 模式的 Calico 额外多出了封包和拆包的过程，集群的网络性能受到了影响，所以在集群的二层网络通的情况下，建议不要使用 IPIP 模式。</p>
<p>看下 node1 上的路由信息：</p>
<pre><code>~ ip routedefault via 192.168.50.1 dev ens33 proto static metric 10010.244.104.0/26 via 192.168.50.11 dev tunl0 proto bird onlink10.244.135.0/26 via 192.168.50.12 dev tunl0 proto bird onlinkblackhole 10.244.166.128/26 proto bird10.244.166.129 dev calif3c799362a5 scope link172.17.0.0/16 dev docker0 proto kernel scope link src 172.17.0.1192.168.50.0/24 dev ens33 proto kernel scope link src 192.168.50.10 metric 100
</code></pre>
<p>可以看到，与之前不一样的是，目的 IP 为 node2 上的 Pod 的数据包是经由 tunl0 发送到网关 192.168.50.11。</p>
<h4 id="抓包验证-3">抓包验证</h4>
<p>从 node1 上的 pod1 发送一个 http 请求到 node2 上的 pod2。</p>
<p>集群网络环境如下：</p>
<pre><code>node1 网卡 ens33：192.168.50.10node1 tunl0：10.244.166.128/32pod1 ip：10.244.166.129
node2 网卡 ens33：192.168.50.11pod2 ip：10.244.104.2node2 tunl0：10.244.104.0/32
</code></pre>
<p>tunl0 设备的抓包情况：</p>
<figure data-type="image" tabindex="9"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3IPLqVnmKWibBdgX8qSwKIrRiaPfya4QMvaqHhS9F4nfqlX0lUsRjZicJw/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>node1 网卡 ens33 的抓包情况：</p>
<figure data-type="image" tabindex="10"><img src="https://mmbiz.qpic.cn/mmbiz_jpg/0o3N73q0BCqaiaGwqCeyaYTeEZvrSKXs3A3cCXoDa3GpyMoUaMYHwt6CdCvyl5Tulq8AibgBvznjouKCibZFYERhg/640?wx_fmt=jpeg&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></figure>
<p>可以看到，IP 包在 tunl0 设备中被封装进了另一个 IP 包，其目的 IP 为 node2 的 IP，源 IP 为 node1 的 IP。</p>
<h2 id="总结">总结</h2>
<p>Kubernetes 的集群网络插件实现方案有很多种，本文主要分析了社区比较常见的两种 Flannel 和 Calico 的工作原理，针对集群内不同节点的 pod 间通信的场景，抓包分析了网络包的走向。</p>
<p>Flannel 主要提供了 Overlay 的网络方案，UDP 模式由于其封包拆包的过程涉及了多次上下文的切换，导致性能很差，逐渐被社区抛弃；VXLAN 模式的封包拆包过程均在内核态，性能要比 UDP 好很多，也是最经常使用的模式；host-gw 模式不涉及封包拆包，所以性能相对较高，但要求节点间二层互通。</p>
<p>Calico 主要采用了 BGP 协议交换路由，没有采用 cni0 网桥，当二层网络不通的时候，可以采用 IPIP 模式，但由于涉及到封包拆包的过程，性能相对较弱，与 Flannel 的 VXLAN 模式相当。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[搭建私有Charts仓库]]></title>
        <id>https://jwangkun.github.io/post/j2GK2Y38c/</id>
        <link href="https://jwangkun.github.io/post/j2GK2Y38c/">
        </link>
        <updated>2020-10-12T12:39:06.000Z</updated>
        <summary type="html"><![CDATA[<p>在 centos 上用 nginx 镜像创建一个容器，名字为 c1</p>
<pre><code>[root@vms12 ~]# docker run -dit --name=c1 -p 8080:80 -v /data:/usr/share/nginx/html/charts  docker.io/nginx 
</code></pre>
<pre><code>ca08a2ce9b8e910ed71f458fa3c7dd53843bf50e5bb92c089fdacf7cd65a1657 
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<p>在 centos 上用 nginx 镜像创建一个容器，名字为 c1</p>
<pre><code>[root@vms12 ~]# docker run -dit --name=c1 -p 8080:80 -v /data:/usr/share/nginx/html/charts  docker.io/nginx 
</code></pre>
<pre><code>ca08a2ce9b8e910ed71f458fa3c7dd53843bf50e5bb92c089fdacf7cd65a1657 
</code></pre>
<!-- more -->
<p>在 master 上自定义一个 chart：</p>
<pre><code>[root@vms10 ~]# mkdir mychar 

[root@vms10 ~]# 

[root@vms10 ~]# cd mychar/ 

[root@vms10 mychar]# helm create chart1 
</code></pre>
<pre><code>Creating chart1 
</code></pre>
<pre><code>[root@vms10 mychar]# cp -r ../mysql . 

[root@vms10 mychar]# 

[root@vms10 mychar]# ls 
</code></pre>
<pre><code>chart1 mysql 
</code></pre>
<p>对这两个 chart 进行打包：</p>
<pre><code>[root@vms10 mychar]# **helm package chart1** 
</code></pre>
<pre><code>Successfully packaged chart and saved it to: /root/mychar/chart1-0.1.0.tgz 
</code></pre>
<p>[root@vms10 mychar]# h<strong>elm package mysql/</strong></p>
<pre><code>Successfully packaged chart and saved it to: /root/mychar/mysql-1.6.4.tgz 
</code></pre>
<pre><code>[root@vms10 mychar]# ls 
</code></pre>
<pre><code>chart1 chart1-0.1.0.tgz mysql mysql-1.6.4.tgz 
</code></pre>
<p>给当前目录下的两个包建立索引文件，并指定私有仓库地址：</p>
<pre><code>[root@vms10 mychar]# helm repo **index** . --url **http://192.168.26.12:8080/charts** 
</code></pre>
<pre><code>[root@vms10 mychar]# ls 
</code></pre>
<pre><code>chart1 chart1-0.1.0.tgz index.yaml mysql mysql-1.6.4.tgz 
</code></pre>
<p>把当前目录下 index.yaml 和后缀为 tgz 的包全部拷贝 192.168.26.12 的/data 目录里（请理解</p>
<p>前面 c1 容器数据卷的设置）：</p>
<pre><code>[root@vms10 mychar]# scp index.yaml *.tgz 192.168.26.12:/data 
</code></pre>
<p>切换到 vms12 上：</p>
<pre><code>[root@vms12 ~]# **ls /data/** 
</code></pre>
<pre><code>index.yaml mychartxx-0.1.0.tgz mysql-1.6.2.tgz 
</code></pre>
<pre><code>[root@vms12 ~]# docker exec -it c1 

&gt; **ls /usr/share/nginx/html/charts** 
</code></pre>
<pre><code>index.yaml mychartxx-0.1.0.tgz mysql-1.6.2.tgz 
</code></pre>
<p>切换到 master 上，添加 http://192.168.26.12:8080/charts 作为仓库：</p>
<pre><code>[root@vms10 mychar]# **helm repo add myrepo http://192.168.26.12:8080/charts** 
</code></pre>
<pre><code>&quot;myrepo&quot; has been added to your repositories 
</code></pre>
<pre><code>[root@vms10 mychar]# helm repo list 
</code></pre>
<p>搜索 mysql 的 chart：</p>
<p>[root@vms10 mychar]# helm search repo mysql</p>
<p>除了在阿里云里可以找到 mysql 的仓库之外，在我们自定义的仓库里也能找到 mysql。</p>
<p>查询 chart1：</p>
<p>[root@vms10 mychar]# helm search repo chart1</p>
<pre><code>NAME 

CHART VERSION APP VERSION DESCRIPTION 

myrepo/chart1 

0.1.0 

1.16.0 

A Helm chart for Kubernetes 
</code></pre>
<p>私有仓库配置完毕。</p>
<p>删除本地私有仓库地址：</p>
<pre><code>[root@vms10 mychar]# **helm repo remove myrepo** 
</code></pre>
<pre><code>&quot;myrepo&quot; has been removed from your repositories 
</code></pre>
]]></content>
    </entry>
</feed>